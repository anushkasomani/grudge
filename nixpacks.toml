[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt",
  "cd acp_sidecar && npm ci"
]

[start]
cmd = "/opt/venv/bin/python web/server.py"
